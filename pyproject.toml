[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "meeting-transcriber"
version = "0.1.0"
description = "Local, speaker-attributed German meeting transcription"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = {text = "Apache-2.0"}
dependencies = [
  "accelerate==1.12.0",
  "librosa==0.11.0",
  "mistral-common[audio]==1.10.0",
  "numpy==2.2.6",
  "pyannote.audio==4.0.7",
  "soundfile==0.14.0",
  "torch==2.8.0",
  "torchaudio==2.8.0",
  "torchcodec==0.7.0",
  "transformers==5.13.0",
]

[project.optional-dependencies]
dev = [
  "mypy==1.18.2",
  "pytest==8.4.2",
  "ruff==0.12.12",
]

[project.scripts]
meeting-transcriber = "meeting_transcriber.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/meeting_transcriber"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src/meeting_transcriber"]
exclude = ["tests"]

[[tool.mypy.overrides]]
module = ["pyannote.*", "soundfile.*", "torch.*", "torchaudio.*", "transformers.*"]
ignore_missing_imports = true
